-- FreshCart Market landing page update
-- No database schema changes are required because the landing page is static frontend content.
-- This ordered no-op migration documents the deploy state for environments that expect a migration artifact per change.
SELECT 1;
